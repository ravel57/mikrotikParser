services:
  web:
    build: .
    links:
      - 'db:db'
    ports:
      - '8080:8080'
    environment:
      - MIKROTIK_PASSWORD=
      - MIKROTIK_USER=
      - GATEWAY=
    depends_on:
      - db
  db:
    image: postgres:14.1-alpine
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - '5432:5432'